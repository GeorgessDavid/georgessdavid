export const MySQLLogo = ({ className }: { className?: string }) => {
    return (
        <span className={`${className} material-symbols-outlined`} style={{ fontSize: '2rem' }}>
            database
        </span>
    )
}
